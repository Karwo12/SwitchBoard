<Window x:Class="SwitchBoard.Views.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="820"
        Height="620"
        MinWidth="680"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource ProcessPicker.Heading}" />
            <TextBlock Margin="0,4,0,0"
                       Style="{StaticResource CaptionText}"
                       Text="{DynamicResource ProcessPicker.Description}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     AutomationProperties.Name="{DynamicResource Common.Search}"
                     ToolTip="{DynamicResource ProcessPicker.SearchHint}" />
            <Button Grid.Column="2"
                    Click="RefreshButton_OnClick"
                    Content="{DynamicResource Common.Refresh}" />
        </Grid>

        <Border Grid.Row="4" Padding="8" Style="{StaticResource GlassCard}">
            <Grid>
                <ListBox x:Name="ProcessList"
                         ItemsSource="{Binding Processes}"
                         SelectedItem="{Binding SelectedProcess, Mode=TwoWay}"
                         SelectionChanged="ProcessList_OnSelectionChanged"
                         MouseDoubleClick="ProcessList_OnMouseDoubleClick">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Margin" Value="0,0,0,5" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="7,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="34" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Image Width="22"
                                       Height="22"
                                       VerticalAlignment="Top"
                                       Source="{Binding Icon}" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" />
                                    <TextBlock Margin="0,3,0,0" Style="{StaticResource CaptionText}">
                                        <Run Text="{Binding ProcessName}" />
                                        <Run Text="  •  " />
                                        <Run Text="{Binding ExecutableName}" />
                                    </TextBlock>
                                    <Grid Margin="0,3,0,0">
                                        <TextBlock Style="{StaticResource CaptionText}"
                                                   Text="{Binding ExecutablePath}" />
                                        <TextBlock Style="{StaticResource CaptionText}"
                                                   Text="{DynamicResource Common.Unavailable}"
                                                   Visibility="{Binding ExecutablePath, Converter={StaticResource NullToVisibilityConverter}}" />
                                    </Grid>
                                </StackPanel>
                                <TextBlock Grid.Column="2"
                                           Margin="14,0,0,0"
                                           VerticalAlignment="Top"
                                           Style="{StaticResource CaptionText}"
                                           Text="{Binding ProcessId, StringFormat=PID: {0}}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ProgressBar Width="220" Height="4" IsIndeterminate="True" />
                    <TextBlock Margin="0,10,0,0"
                               HorizontalAlignment="Center"
                               Style="{StaticResource CaptionText}"
                               Text="{DynamicResource ProcessPicker.Loading}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Style="{StaticResource CaptionText}"
                       Text="{Binding StatusText}" />
            <Button Grid.Column="1"
                    Click="CancelButton_OnClick"
                    Content="{DynamicResource Common.Cancel}" />
            <Button x:Name="SelectButton"
                    Grid.Column="3"
                    MinWidth="112"
                    IsEnabled="False"
                    Click="SelectButton_OnClick"
                    Content="{DynamicResource Common.Select}"
                    Style="{StaticResource AccentButton}" />
        </Grid>
    </Grid>
</Window>
